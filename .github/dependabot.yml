version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current; without this, pinning freezes
  # them and they miss upstream security fixes.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
